{
  "schemaVersion": 1,
  "symlinks": {
    "CLAUDE.md": "AGENTS.md"
  },
  "afterCreate": ["apm install", "pnpm install"],
  "afterUpdate": [
    "pnpm install",
    "pnpm format:check",
    "pnpm typecheck",
    "pnpm test",
    "pnpm test:e2e",
    "pnpm build"
  ]
}
